# Root conftest: makes bare `pytest` work from the repo root by putting this
# directory on sys.path (so `import researcher` / `research.lib` / `deliverables`
# resolve without installing the package). Test config lives in pyproject.toml.
