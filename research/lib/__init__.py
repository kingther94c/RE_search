"""Importable harvest harness: mbx (adb UI core) + table harvesters + reconstruct_comps.

Run as modules from the repo root (``python -m research.lib.harvest_sale gallop``)
or import the pure parsers (``from research.lib.harvest_sale import parse_sale_texts``).
Harvest outputs land in ``research/data/``; captures in ``research/captures/``.
"""
